package yega.alogorithms.randomizecontraction;

import org.apache.log4j.Logger;
import org.junit.Test;


public class MinCutAlgorithmTest {
	private static Logger LOG = Logger.getLogger(RandomizeContraMinCutAlgorithmTestcionAlgorithm.class);
	
	@Test
	public void verifyThantMinCuntWorks() {
		MinCutAlgorithm algo = new MinCutAlgorithm();
		
		Integer size = algo.find(RandomizeContractionAlgorithmTestData.EDGES);
		
		LOG.info(String.format("Min Cun on this run is [%d]", rcAlgo.getEdges().size()));
	}
}
